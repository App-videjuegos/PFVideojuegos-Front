import { View, Text } from 'react-native'
import React from 'react'

const UserProfile = ({ navigation, route }) => {
  return (
    <View>
      <Text>este es el componente UserProfile</Text>
    </View>
  )
}

export default UserProfile